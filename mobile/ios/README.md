# SoundOrbit iOS (Phase 2)

Not built yet. Planned:

- **Metal** primary renderer (or Vulkan via **MoltenVK** once a shared core exists)
- Mic via `AVAudioEngine` (same limit as Android: **not** system audio)
- Sideload IPA requires Apple signing (free team / paid developer) + macOS Xcode or CI

See `../README.md` for product limits.
